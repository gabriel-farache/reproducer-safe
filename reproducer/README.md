Workflow is getting files to be pushed to a github branch and creates a PR: start MockServer.py, it will return the full sample response. MockServer_light.py returns only few files.

Run by executing `mvn clean quarkus:dev`.